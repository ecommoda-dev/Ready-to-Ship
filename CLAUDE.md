# قائمة أوردرات القاهرة — جاهزة للشحن (`Ready-to-Ship`)

**بتعمل إيه:** بتجيب قايمة أوردرات القاهرة الجاهزة للشحن (S1/S2) من Shopify، وبتدوّر على أوردرات مسحوبة غير متوقعة بالاسم أو الـ ID.
**مين بيستخدمها:** مخزن
**الإصدار:** Worker `v1.0.0` · الواجهة `v1.0.0`   ← الاتنين مستقلين، طبيعي يختلفوا

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Ready-to-Ship/
الـ Worker : https://ready-to-ship-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: ready-to-ship-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `get_ready_orders` | GET — قايمة أوردرات S1/S2 الجاهزة للشحن |
| `lookup_orders` | POST — بحث عن أوردرات بالاسم أو الـ ID (للأوردرات المسحوبة غير المتوقعة) |

## D1

```
مفيش. الأداة دي مالهاش تسجيل D1 أصلاً — مفيش login/logout ولا audit log
(موثّق في هيدر index.js نفسه).
```

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : لا يوجد (مفيش D1)
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET
Vars     : SHOP_DOMAIN   ← من [vars] في wrangler.toml
Build watch paths : * الافتراضي
```

## CORS

`wildcard *` — لأن الأداة قراءة فقط (read-only dashboard)، مفيش كتابة ولا بيانات حساسة في الردود.

## خط الأساس بعد النقل

> الأرقام اللي الأداة رجّعتها بعد ما اتأكدنا إنها شغالة — مرجع لأي شك بعد كده.

```
اتنقلت بلا خط أساس — بند ١٠ (§11 في ecommoda-tool-migration-playbook) مفتوح بوعي.
أحمد لازم يضغط "تحديث" في الأداة بعد الربط ويسجّل الأرقام هنا.
```

## فخاخ الأداة دي

- مفيش D1 خالص في الأداة دي — فلو حد ضاف `[[d1_databases]]` في `wrangler.toml` بالغلط قياسًا على أدوات تانية، الكود مش بيستخدم `env.DB` أصلاً فهيبقى binding زيادة بلا فايدة، مش خطأ وظيفي.
- `lookup_orders` بيفرّق بين اسم أوردر عادي وID مخزّن كاسم عرض بطول الأرقام (>10 رقم) — لو ده اتغيّر في منطق شوبيفاي يوم ما، الفلترة دي محتاجة مراجعة.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
مفيش نسخ مرقّمة قديمة في الريبو ده وقت النقل — الملف الوحيد كان Index.html.
```

## بصمة المهارات

> الصيغة والقواعد والمهارات اللي بتدخل الجدول → `ecommoda-skill-versioning`
> Step 4. مهارة مالهاش رقم إصدار مابتدخلش الجدول.

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v1.0.0 |
| ecommoda-html-builder | v2.0.0 |
| ecommoda-constants | v1.2.0 |

آخر مطابقة: 30-08-2026 · `index.js` v1.0.0 · `index.html` v1.0.0
🔴 معلّقة: — لا شيء

> سطر **🔴 معلّقة** = أي بند كاسر **معروف ومتقرر تأجيله**، بسببه.
> `— لا شيء` معناها مفيش. **بند 🔴 متأجل من غير ما يتكتب هنا = بند ضايع** —
> مفيش ملف تاني في المشروع بيتتبّعه.

## مسائل مفتوحة

- خط الأساس بعد النقل (فوق) لسه مفتوح — محتاج أحمد يسجّله بعد أول تحديث للأداة بعد الربط.
